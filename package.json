{
  "name": "root",
  "private": true,
  "scripts": {
    "compile": "npx wsrun --serial compile",
    "watch": "npx tsc -b -w"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**"
    ]
  },
  "devDependencies": {
    "typescript": "^3.6.2",
    "wsrun": "^5.0.0"
  }
}
